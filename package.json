{
  "name": "epl-match-simulator",
  "version": "1.0.0",
  "description": "EPL Match Simulator - Flutter Web app with Claude API",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "vercel-build": "bash ./vercel-build.sh",
    "build-local": "cd epl_match_simulator && flutter pub get && flutter build web --release && cd ..",
    "test-build": "bash ./vercel-build.sh"
  }
}
